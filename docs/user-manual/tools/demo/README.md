# Demo fixture

`alps-hotel.sh3d` is the Sweet Home 3D plan used to generate the screenshots in the
user manual. It is a generic example model (an Alpine hotel), used purely as demo
geometry.

The screenshot generator runs the Walldash backend with **no Home Assistant
configuration**, which makes it serve its built-in example devices. Nothing from a
real installation is ever used.
